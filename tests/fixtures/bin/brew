#!/usr/bin/env bash

set -euo pipefail

if [[ "${1:-}" == 'list' && "${2:-}" == '--cask' ]]; then
  exit 1
fi

if [[ "${1:-}" == 'bundle' && "${2:-}" == 'check' ]]; then
  exit "${BATS_BREW_BUNDLE_STATUS:-0}"
fi

exit 0
