#!/usr/bin/env bash

set -euo pipefail

if [[ "${1:-}" == 'install' && "${2:-}" == '--dry-run-code' ]]; then
  exit "${BATS_MISE_VERIFY_STATUS:-0}"
fi

if [[ "${1:-}" == 'install' ]]; then
  exit "${BATS_MISE_INSTALL_STATUS:-0}"
fi

exit 0
